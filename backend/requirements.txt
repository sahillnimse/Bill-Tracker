fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.5

# AWS
boto3==1.35.97

# RunPod
httpx==0.28.1

google-auth==2.38.0

# Google Ads
google-ads==31.1.0

# Google Workspace (Admin SDK Reports API)
google-api-python-client==2.163.0

# Microsoft 365 + Outlook (Graph)
msal==1.31.1

# Auth (TOTP login)
PyJWT==2.10.1
pyotp==2.9.0
qrcode[pil]==8.0

# OAuth flow helper (used by generate_refresh_token.py)
google-auth-oauthlib==1.2.1

# Postgres (production DB on Render — used automatically when DATABASE_URL is set)
psycopg2-binary==2.9.12
apscheduler==3.10.4